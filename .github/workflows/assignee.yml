name: Review Assign

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: hkusu/review-assign-action@v1
        with:
          assignees: ${{ github.actor }}
          reviewers: ilon-mars, MoonLightFoxKs, sayanasay, tulolo287, RNemch
          max-num-of-reviewers: 2
          github-token: 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
